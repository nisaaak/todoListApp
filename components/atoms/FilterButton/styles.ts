import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        borderWidth: 2,
        borderColor: '#F48E8E',
        borderRadius: 8,
        paddingHorizontal: 10,
        marginHorizontal: 10
    }
})