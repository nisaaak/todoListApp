import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button: {
        marginTop: 20,
        borderRadius: 20,
        height: 50,
        justifyContent: 'center',
        alignItems: 'center'
    }
})