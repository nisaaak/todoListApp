import { StyleSheet } from "react-native";

export default StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: '#868fba',
        backgroundColor: '#fff',
        borderRadius: 8,
        padding: 10
    }
})