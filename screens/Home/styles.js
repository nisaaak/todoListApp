import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        justifyContent: 'space-between',
        flex: 1
    },
    search: {
        flexDirection: 'row',
        marginTop: 10
    }
})